# Example GitLab served-repo config (copy to <repo_id>.yaml and edit).
# Secret: GITLAB_TOKEN_YOUR_GROUP (optional override MAJORDOMO_CREDENTIAL_EXAMPLE_GITLAB).
# Org defaults live in _defaults.yaml — only override what differs.

scm: gitlab

repository:
  id: example-gitlab
  # Nested groups: owner can be "group/subgroup"
  owner: YOUR_GROUP
  name: YOUR_PROJECT
  cloneUrl: https://gitlab.com/YOUR_GROUP/YOUR_PROJECT.git

scmApi:
  baseUrl: https://gitlab.com
  # Numeric project id OR leave empty to use owner/name (URL-encoded path)
  # projectId: "12345"
